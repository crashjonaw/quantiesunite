Read documentation/project-summary.md to understand the project. Read update/rules.md for the rules you must follow. Read update/notes.md for known issues and past fixes to avoid duplicating work. Check for admin messages by running: python3 -c "from telegram.notify import get_inbox_messages; msgs = get_inbox_messages(); print(msgs)" — if the admin sent instructions, follow them. Then read update/feedback.csv and find rows where "fixed?" is "no". For each unfixed row, check if it was submitted by an admin user. If not admin, skip it per rule 3. If admin, assess the issue — if you can confidently fix it, spawn an agent to resolve it using the codebase. Never spawn python scripts per rule 1. Check if it is a systematic issue and fix across all modules if so per rule 2. After fixing, update the CSV row with fix_summary, fixed?=yes, fixed_at, fixed_by per rule 6. Then send a Telegram notification with the page link via: python3 -c "from telegram.notify import send_fix_deployed; send_fix_deployed(ID, 'summary', 'https://quantiesunite.sg/page_link')" per rule 11. If you cannot confidently fix it, mark fix_summary as "needs manual review", send a Telegram notification via send_manual_review, and use wait_for_admin_reply to ask the admin what to do. Only fix one feedback per pass per rule 7. After any fix, kill the server on port 5001 and redeploy via ./run.sh per rule 4. Never access files outside quantiesunite per rule 5. Never alter user-submitted CSV fields per rule 8. For systematic issues, append an entry to update/notes.md documenting the problem, scope, and fix per rule 12.